apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: docker-registry
  namespace: devops
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: docker-registry
  updatePolicy:
    updateMode: "Off"
