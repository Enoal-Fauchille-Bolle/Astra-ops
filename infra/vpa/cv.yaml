apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: cv
  namespace: web
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: cv
  updatePolicy:
    updateMode: "Off"
