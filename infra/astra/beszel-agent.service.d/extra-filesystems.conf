# Drop-in for the Beszel agent, installed as
# /etc/systemd/system/beszel-agent.service.d/extra-filesystems.conf
# Reports /mnt/pve/vault, which holds every backup, so a disk alert can watch it.
[Unit]
After=mnt-pve-vault.mount

[Service]
Environment="EXTRA_FILESYSTEMS=/mnt/pve/vault"
