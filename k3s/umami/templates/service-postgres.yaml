apiVersion: v1
kind: Service
metadata:
  name: umami-postgres-svc
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: umami-postgres
  ports:
    - name: postgres
      port: {{ .Values.postgres.service.port }}
      targetPort: {{ .Values.postgres.service.port }}
