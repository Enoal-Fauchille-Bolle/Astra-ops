{{- if .Values.ingress.dashboard.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: umami-dashboard-ingress
  namespace: {{ .Values.namespace }}
spec:
  rules:
    {{- range .Values.ingress.dashboard.hosts }}
    - host: {{ . }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: umami-app-svc
                port:
                  number: {{ $.Values.app.service.port }}
    {{- end }}
{{- end }}
