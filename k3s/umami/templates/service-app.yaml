apiVersion: v1
kind: Service
metadata:
  name: umami-app-svc
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: umami-app
  ports:
    - name: http
      port: {{ .Values.app.service.port }}
      targetPort: {{ .Values.app.service.targetPort }}
