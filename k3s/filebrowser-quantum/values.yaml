namespace: media

image:
  repository: gtstef/filebrowser
  tag: 1.5.6-stable
  pullPolicy: IfNotPresent

replicaCount: 1

strategy:
  type: Recreate

service:
  port: 80
  targetPort: 80

ingress:
  enabled: true
  hosts:
    - drive.enoal.fr
    - filebrowser-quantum.lan

volumes:
  files:
    hostPath: /mnt/data/k3s-pvc/filebrowser
    mountPath: /srv/files
  media:
    hostPath: /mnt/data/media
    mountPath: /srv/Media
  # The phone backups only: the rest of /mnt/data/backups holds the database dumps and the
  # Proxmox configuration copy, secrets this root container could read and serve
  backups:
    hostPath: /mnt/data/backups/OnePlus-10T
    mountPath: /srv/Backups/OnePlus-10T
  db:
    hostPath: /opt/k3s-data/filebrowser-quantum
    mountPath: /home/filebrowser/data/

securityContext:
  runAsUser: 0

resources:
  requests:
    cpu: 10m
    memory: 96Mi
  limits:
    cpu: 200m
    memory: 512Mi
