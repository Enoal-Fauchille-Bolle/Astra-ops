namespace: web

image:
  repository: ghcr.io/enoal-fauchille-bolle/portfolio
  tag: latest
  pullPolicy: Always

replicaCount: 1

strategy:
  type: RollingUpdate
  rollingUpdate:
    maxUnavailable: 0
    maxSurge: 1

service:
  port: 80
  targetPort: 80

ingress:
  enabled: true
  host: portfolio.enoal.fr

resources:
  requests:
    cpu: 10m
    memory: 64Mi
  limits:
    cpu: 100m
    memory: 128Mi

imagePullSecrets:
  - name: regcred-portfolio
