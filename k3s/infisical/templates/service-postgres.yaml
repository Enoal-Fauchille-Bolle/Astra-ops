apiVersion: v1
kind: Service
metadata:
  name: infisical-postgres-svc
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: infisical-postgres
  ports:
    - name: postgres
      port: {{ .Values.postgres.service.port }}
      targetPort: {{ .Values.postgres.service.port }}
