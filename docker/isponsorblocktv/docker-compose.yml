services:
  isponsorblocktv:
    image: ghcr.io/dmunozv04/isponsorblocktv:v2.11.0
    container_name: isponsorblocktv
    restart: unless-stopped
    volumes:
      - /opt/docker-data/isponsorblocktv:/app/data
