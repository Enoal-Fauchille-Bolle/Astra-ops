services:
  zerobyte:
    image: ghcr.io/nicotsx/zerobyte:v0.35
    container_name: zerobyte
    restart: unless-stopped
    cap_add:
      - SYS_ADMIN
    ports:
      - "4096:4096"
    devices:
      - /dev/fuse:/dev/fuse
    env_file: .env
    environment:
      - TZ=Europe/Paris
      - BASE_URL=http://zerobyte.lan
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /var/lib/zerobyte:/var/lib/zerobyte
